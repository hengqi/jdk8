1.AbstractPipeline是一个抽象的管道类的基类，也是流接口及原生特化接口（IntStream等）的核心实现。用于管理流管道的构建和计算。
2.一个AbstractPipeline代表一个流管道的初始部分，封装了一个流的源和零个或多个中间操作。
每一个AbstractPipeline对象通常被称为阶段，每一个阶段要么描述的是源，要么描述的是一个中间操作。
3.一个具体的中间阶段通常是从一个AbstractPipeline来构建的，AbstractPipeline包含了大多数的管道计算，并且实现方法将会被这些操作所用到。一个特化的流管道避免了自动装箱和拆箱的操作。
4.在链接完一个新的中间操作或者是执行了一个终止操作之后，流就被认为已消费了。并且不允许在此流上添加中间操作和终止操作。